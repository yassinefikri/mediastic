{% extends 'user/index.html.twig' %}

{% block account_extra %}
    <div class="mb-5">
        <div id="account_cover">
            <img class="w-100 h-100" src="{{ app.user.cover ? asset(app.user.cover, 'covers') : asset('default_cover.jpeg', 'images')}}"/>
            <img class="rounded-circle" id="account_avatar" src="{{ app.user.avatar ? asset(app.user.avatar, 'avatars') : asset('default_avatar.png', 'images')}}" >
        </div>
    </div>
    <br>
    <br>
{% endblock %}